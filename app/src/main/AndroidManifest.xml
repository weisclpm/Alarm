<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weisc.alarm">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".MyReceiver"
            android:process=":remote">
            <intent-filter>
                <action android:name="com.weisc.alarm"></action>
            </intent-filter>

        </receiver>

        <activity android:name=".SetAlarmActivity" />
        <activity android:name="com.example.weisc.settings.RepeatSettings" />

        <service
            android:name="com.example.weisc.services.AlarmService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>